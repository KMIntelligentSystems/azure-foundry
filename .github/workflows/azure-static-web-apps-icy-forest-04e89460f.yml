name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - azure-foundary
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - azure-foundary

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        env:
          VITE_AGENT_WS_URL: wss://foundry-orchestrator-gateway.bravesea-f16a8310.eastus.azurecontainerapps.io/ws/agent
          VITE_ENTRA_TENANT_ID: 861d72fc-cfd3-4e7c-b2cb-128bc7b85add
          VITE_ENTRA_CLIENT_ID: e9a5563e-df5e-47df-816b-ba4ab9501904
          VITE_GATEWAY_SCOPE: api://094bea17-5f7e-4ab9-8156-1aac4b4650ad/access_as_user
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ICY_FOREST_04E89460F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "src/react-app" # App source code path
          api_location: "src/api" # Api source code path - optional
          output_location: "dist" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ICY_FOREST_04E89460F }}
          action: "close"
